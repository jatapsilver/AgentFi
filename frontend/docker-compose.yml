services:
  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: frontend
    env_file:
      - .env
    ports:
      - "8080:8080"
    networks:
      - app-front
networks:
  app-front:
    driver: bridge
