services:
  frontend:
    build:
      context: .
      dockerfile: ./Dockerfile
    container_name: frontend
    env_file:
      - .env
    ports:
      - "8080:8080"
    networks:
      - app-net
networks:
  app-net:
    external: true
